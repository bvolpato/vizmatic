interface:
  display_name: "Vizmatic"
  short_description: "Create diagrams and charts from TSX"
  default_prompt: "Use $vizmatic to create a theme-aware diagram and render dark and light PNG files."

policy:
  allow_implicit_invocation: true
